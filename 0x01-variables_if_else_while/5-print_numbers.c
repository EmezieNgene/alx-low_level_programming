#include <stdio.h>

/**
 * main - prints all single digit numbers in base 10 starting from 0
 * Return: 0
 */

int main(void)
{
	int nums;
	while (nums < 10)
	{
		printf("%d, nums");
		nums += 1;
	}
	printf("\n");
	return (0);
}

